language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ytx-vue' ]; then cd .. && eval "mv $currentfolder generator-ytx-vue" && cd generator-ytx-vue; fi
